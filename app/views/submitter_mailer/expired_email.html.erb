<p><%= t('hi_there') %>,</p>
<p><%= t('the_document_name_has_expired_without_being_signed_by_all_parties', name: @submission.name || @submission.template&.name) %></p>
<p><%= link_to submission_url(@submission), submission_url(@submission) %></p>

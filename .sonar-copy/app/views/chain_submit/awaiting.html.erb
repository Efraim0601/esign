<% content_for(:page_title) { t('awaiting_completion_by_the_other_party') } %>
<div class="flex items-center justify-center min-h-screen px-4 bg-base-200">
  <div class="w-full max-w-md bg-base-100 rounded-2xl shadow-xl p-8 text-center">
    <h1 class="text-xl font-semibold mb-4"><%= t('awaiting_completion_by_the_other_party') %></h1>
    <p class="text-sm text-base-content/70">
      <%= t('chain_awaiting_message') %>
    </p>
  </div>
</div>
